@model IEnumerable<FinalCapstone.Models.Request>

@{
    ViewBag.Title = "Index";
}
    <div class="jumbotron" style="background-image: url(../../streetlifeboots2.jpg); object-fit: fill; background-size: cover; background-position:center; height: 200px">
        <p class="lead" style="font-size: 75px; text-shadow: -4px 4px 5px #000000;
">Requests</p>
    </div>

<p class="btn btn-default">
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Subject)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MessageToPost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DatePosted)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Subject)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MessageToPost)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DatePosted)
        </td>
        <td>
            <div class="btn btn-default">
                @Html.ActionLink("Edit", "Edit", new { id = item.MessageId })
            </div>
            <div class="btn btn-default">
                    @Html.ActionLink("Details", "Details", new { id = item.MessageId })
            </div>
            <div class="btn btn-default">
                        @Html.ActionLink("Delete", "Delete", new { id = item.MessageId })
            </div>
</td>
    </tr>
}

</table>
